gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Banuba Face AR SDK license token — real vendor secret, never committed.
banuba.properties

# Real Face AR (Banuba SDK) — effect assets are copied here at build time
# (see app/build.gradle.kts's copyBanubaEffects task) from
# mobile/banuba_effects, the actual source of truth.
app/src/main/assets/bnb-resources/
