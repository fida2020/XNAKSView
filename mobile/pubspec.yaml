name: xnakview
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # Routing
  go_router: ^14.6.2

  # API client
  dio: ^5.7.0

  # Secure storage abstraction (tokens, sensitive local data)
  flutter_secure_storage: ^9.2.2

  # Value equality for models/state
  equatable: ^2.0.5

  # Vertical video feed playback
  video_player: ^2.9.2

  # Picking a video from the gallery to upload
  image_picker: ^1.1.2

  # LIVE streaming: WebRTC client for the self-hosted LiveKit server
  livekit_client: ^2.4.2

  # Runtime camera/microphone permission requests for going LIVE
  permission_handler: ^11.3.1

  # Chat + calls (Step 5): real-time transport client matching the backend's
  # Socket.IO gateway (lib/realtime.ts) — see features/messaging/data/realtime_client.dart.
  socket_io_client: ^2.0.3+1

  # Voice message recording and playback
  record: ^7.1.1
  just_audio: ^0.9.42
  path_provider: ^2.1.5

  # Real Google Sign-In / Facebook Login (native SDKs) — the ID/access
  # token they produce is verified server-side (backend lib/oauthProviders.ts),
  # never trusted client-side alone. See docs/ARCHITECTURE.md for the real
  # provider credentials that must be configured before these work on-device.
  google_sign_in: ^6.2.2
  flutter_facebook_auth: ^7.1.1

  # Real in-app camera preview/recording for Create (Step: Create rebuild) —
  # front/back switch, flash, zoom are genuine native camera capabilities,
  # not a fake/simulated recorder.
  camera: ^0.11.1

  # Real Face AR — beautify/retouch, AR effects (Banuba Face AR SDK,
  # on-device, no cloud dependency). banuba_sdk 3.1.6's own Android module
  # pins compileSdkVersion 31 internally, which conflicts with newer
  # transitive androidx artifacts this project's other native plugins
  # already require — worked around with a scoped per-subproject compileSdk
  # override in android/build.gradle.kts (same targeted-override pattern
  # already used there for JVM target alignment), not a blind global change.
  # The license token is never hard-coded here — see
  # core/config/app_config.dart, read via --dart-define at build time.
  banuba_sdk: ^3.1.6

  # Native Android/iOS share sheet — the OS itself detects which compatible
  # apps are actually installed (WhatsApp, Messenger, ...), so this never
  # hardcodes a fixed app list.
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates the real per-density Android/iOS launcher icon files from the
  # existing XNAKView logo (assets/branding/xnakview_logo.png) — run via
  # `dart run flutter_launcher_icons`. Dev-only; not part of the app's
  # runtime dependency graph.
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/branding/xnakview_logo.png"
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/branding/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
